/* tslint:disable:no-unused-variable */

import {ColorsComponent} from './colors.component';

describe('Component: Colors', () => {
  it('should create an instance', () => {
    const component = new ColorsComponent();
    expect(component).toBeTruthy();
  });
});
